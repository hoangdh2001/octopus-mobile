import 'package:flutter/material.dart';

class ChannelPreviewStatus extends StatelessWidget {
  const ChannelPreviewStatus({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
